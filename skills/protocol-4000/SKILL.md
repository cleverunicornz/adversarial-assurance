---
name: protocol-4000
description: Use when an exact pushed contract Lock and a separate human-agreed pushed 4000 Closure commission implementation and its bounded 4030 assurance. Never inherit 3000 rounds or manufacture predecessor, successor, review, skill, documentation, materialization, or re-assurance work.
node: protocol-4000
class: skill
edges:
  - type: cites
    target: git-policy
    provenance: declared
  - type: cites
    target: protocol-trunk-and-leaf
    provenance: declared
  - type: cites
    target: underwrite
    provenance: declared
metadata:
  short-description: Deliver under one separate implementation Closure
---

# Lane 4000 - Implementation

The lane where possibilities collapse into behavior against one exact contract
Lock. Before implementation begins, the human agrees and the orchestrator
Posts a separate block-4000 Closure through `$underwrite`. It names that Lock,
maps acceptance rows to the known tasks, and counts only 4030 passes.

Refusal: missing or mismatched Lock, absent pushed 4000 Closure, a cross-block
ordinal, an unauthorized governing edit, or exhausted allowance. Preserve the
exact evidence and stop; the refusal creates no repair, route, predecessor,
successor, or later lane.

Agents never edit the locked contract or either Closure. One exact human
instruction may change governing bytes directly on the active 4000 lineage;
the sole writer commits and pushes them before work resumes. That edit invokes
no review by itself.

Build uses Moon, native language tools, direct binaries, and bounded temporary
processes. It never invokes Nix or turns operational materialization into a
Build prerequisite.

A terminal 4000 result does not commission 5000, 6000, 7000, or 8000. Those
begin only under their own express commission.

Index: `$git-policy` - Lanes.
