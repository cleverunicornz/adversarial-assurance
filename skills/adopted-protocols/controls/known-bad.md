# Known-Bad Control — The Attache Inversion (pinned incident, 2026-08-02)

Reconstructed claim, preserved as the canonical authority-direction
violation. The checker must BLOCK this text, with verbatim quotes, before
any live verdict counts. This control is permanent and is never edited.

---

The conversation-cell campaign is blocked: the Flue attache contract and
its drive API must be delivered before the Matrix protocol surface and the
server can be written. The attache is the component that talks to the
room, so its API shape determines what the agent seam looks like, and the
server's Matrix behavior should be authored against the attache's contract
once it exists. Until Flue publishes that contract, Matrix-side authoring
cannot proceed.

---

Expected verdict: BLOCK.

- Question 2 (authority direction): a consumer's adapter is given
  definitional power over an adopted plane — "its API shape determines
  what the agent seam looks like" — and a consumer delivery timeline gates
  the adopted surface. The arrow points backwards.
- The block must quote the violating sentences verbatim and name the root
  Protocols law and the Matrix register entry. No superior sentence exists
  anywhere in the tree that supports the claim; a block resting on the
  claim would itself be unable to quote one.
