# Known-Good Control — Conformant Consumer Statement

The checker must PASS this text before any live verdict counts. It states
the true form of the same facts the known-bad control inverts. This
control is permanent and is never edited.

---

The cell serves the pinned Matrix v1.19 Client-Server API as the profile
ledger stages it. The attache consumes committed room events as opaque
canonical Ruma bytes over the internal agent seam and calls Flue's public
typed drive API to admit interjections; where the attache's convenience
and Matrix semantics appear to conflict, the attache is the defect and
conforms. The Attache contract locks only after Flue's public drive API
resolves; the server's Matrix surfaces are authored and proven
independently of that timeline.

---

Expected verdict: PASS.

- Consumers conform; the adopted grammar is unconditioned; the sequencing
  sentence gates a consumer artifact on the consumer's own dependency
  without touching the adopted plane's authoring or authority.
